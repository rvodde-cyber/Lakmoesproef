import React, { useState } from 'react';\n\nconst AuditSurvey = () => {\n    const [score, setScore] = useState(0);\n    const [showCQuestion, setShowCQuestion] = useState(false);\n\n    const handleScoreChange = (event) => {\n        const value = parseInt(event.target.value);\n        setScore(value);\n        setShowCQuestion(value < 3);\n    };\n\n    const getSliderColor = (value) => {\n        if (value >= 1 && value <= 2) return 'red';\n        if (value === 3) return 'orange';\n        return 'green';\n    };\n\n    return (\n        <div>\n            <h1>Audit Survey</h1>\n            <label htmlFor="score">Rate our service (1-5):</label>\n            <input\n                type="range"\n                id="score"\n                min="1"\n                max="5"\n                value={score}\n                onChange={handleScoreChange}\n                style={{ backgroundColor: getSliderColor(score) }}\n            />\n            <p>Your score: {score}</p>\n            {showCQuestion && (\n                <div>\n                    <h2>Question C</h2>\n                    <p>Please provide your feedback:</p>\n                    <textarea></textarea>\n                </div>\n            )}  \n        </div>\n    );\n};\n\nexport default AuditSurvey;